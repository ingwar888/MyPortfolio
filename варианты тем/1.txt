добавить css 
/* Добавьте в skillpage.module.css */

.page {
  min-height: 94vh;
  background: #0a0a0a;
  padding: 2rem 2rem 10rem;
  font-family: 'Inter', system-ui, sans-serif;
  position: relative;
  overflow: hidden;
}

/* Анимированные линии кода на фоне */
.page::before {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: 
    /* Горизонтальные линии - код */
    repeating-linear-gradient(
      0deg,
      transparent,
      transparent 40px,
      rgba(0, 120, 215, 0.03) 40px,
      rgba(0, 120, 215, 0.03) 41px
    ),
    /* Вертикальные линии - отступы */
    repeating-linear-gradient(
      90deg,
      transparent,
      transparent 80px,
      rgba(255, 215, 0, 0.02) 80px,
      rgba(255, 215, 0, 0.02) 81px
    );
  pointer-events: none;
  z-index: 0;
  animation: codeScroll 20s linear infinite;
}

@keyframes codeScroll {
  0% {
    transform: translateY(0);
  }
  100% {
    transform: translateY(41px);
  }
}

/* Частицы технологий */
.page::after {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-image: 
    radial-gradient(circle at 20% 30%, rgba(0, 120, 215, 0.1) 0%, transparent 50%),
    radial-gradient(circle at 80% 70%, rgba(255, 215, 0, 0.1) 0%, transparent 50%),
    radial-gradient(circle at 50% 50%, rgba(112, 44, 190, 0.05) 0%, transparent 70%);
  pointer-events: none;
  z-index: 0;
}

/* Контент поверх фона */
.hero, .skillsGrid {
  position: relative;
  z-index: 1;
}